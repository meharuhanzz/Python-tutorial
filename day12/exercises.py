"""Day 12 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""

# 1. Write a function `safe_int(text)` that tries to convert text to an
#    int, catching ValueError and returning None if it fails. Test it
#    with "42" and with "abc".
# TODO

# 2. Write a function `get_item(a_list, index)` that safely returns
#    a_list[index], catching IndexError and returning "Not found" instead
#    of crashing. Test it with an index that's in range and one that
#    isn't.
# TODO

# 3. Write a function `divide_all(numbers, divisor)` that divides every
#    number in a list by divisor and returns the results, but catches
#    ZeroDivisionError and prints a friendly message (returning an empty
#    list) if divisor is 0.
# TODO

# 4. Define a custom exception `NegativeValueError(Exception)`. Write a
#    function `square_root(n)` that raises NegativeValueError if n is
#    negative (you can't take a real square root of a negative number),
#    otherwise returns n ** 0.5. Test it with a positive and a negative
#    number, catching your custom exception around the negative case.
# TODO
