"""Day 3 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""

# 1. Ask the user for their full name with input(). Print it in uppercase,
#    then in lowercase, then print how many characters it has (len()).
# TODO

# 2. Given sentence = "the quick brown fox", use .split() to break it into
#    a list of words, then use ' '.join(...) to put it back together with
#    each word capitalised. (Hint: look up the .capitalize() method too.)
# TODO

# 3. Given a string `s = "racecar"`, write an expression using slicing
#    ([::-1]) to check whether it's a palindrome (reads the same
#    backwards). Test it with "racecar" and "python".
# TODO

# 4. Use an f-string to print your name centered in a line of dashes,
#    e.g. "----Ada----" (look up str.center() for a hint, or do it with
#    slicing/multiplication).
# TODO
