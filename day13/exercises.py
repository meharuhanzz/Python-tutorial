"""Day 13 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""


# 1. Define a class `Car` with __init__ taking make and model, and a
#    method `describe()` that prints "This is a <make> <model>." Create
#    two Car objects and call describe() on each.
# TODO


# 2. Define a class `Counter` with an instance attribute `count` starting
#    at 0, and methods `increment()` (adds 1) and `reset()` (sets back to
#    0). Create a Counter, increment it 3 times, print the count, then
#    reset it and print again.
# TODO


# 3. Define a class `Rectangle` with __init__ taking width and height,
#    and a method `area()` that returns width * height. Give it a
#    __str__ method that returns something like "Rectangle(4 x 5)".
#    Create one and print it, then print its area.
# TODO


# 4. Define a class `Playlist` with an instance attribute `songs` (a
#    list, starting empty), a method `add_song(title)`, and a method
#    `total_songs()` that returns how many songs are in the playlist.
#    Create a playlist, add 3 songs, and print the total.
# TODO
