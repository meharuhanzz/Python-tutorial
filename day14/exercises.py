"""Day 14 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""


# 1. Define a base class `Vehicle` with __init__(self, brand) and a
#    method `move()` that prints "The vehicle moves." Define subclasses
#    `Car` and `Bicycle` that each override move() with something more
#    specific ("The car drives." / "The bicycle pedals."). Create one of
#    each and call move() on both.
# TODO


# 2. Create a list containing a mix of Car and Bicycle objects from
#    exercise 1 (at least 2 of each), then loop over the list calling
#    move() on each -- this is the polymorphism pattern from main.py.
# TODO


# 3. Define a class `Person` with __init__(self, name, age) and a method
#    describe() that prints "<name> is <age> years old." Define a
#    subclass `Student` that adds a `school` attribute (via __init__ and
#    super().__init__()) and overrides describe() to call the parent's
#    describe() with super() AND then also print the school.
# TODO


# 4. Define a base class `PaymentMethod` with a method `pay(amount)` that
#    raises NotImplementedError. Define subclasses `CreditCard` and
#    `Cash`, each implementing pay() to print a different message (e.g.
#    "Paid 50 by credit card." / "Paid 50 in cash."). Create one of each
#    and call pay(50) on both.
# TODO
